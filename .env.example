# ============================
# Variables de entorno para Docker Compose
# Copiar este archivo como .env y ajustar los valores
# cp .env.example .env
# ============================

# PostgreSQL
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_root_password
POSTGRES_DB=postgres

# App Client (usuario con permisos limitados)
APP_CLIENT_PASSWORD=your_app_client_password

# Conexion a la base de datos (usada por el dashboard)
# Nota: dentro de Docker, el host es "db" (nombre del servicio)
DATABASE_URL=postgresql://app_client:your_app_client_password@db:5432/postgres

# Next.js
NODE_ENV=production
NEXT_TELEMETRY_DISABLED=1
